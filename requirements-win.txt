# Windows 版依赖（macOS 版见 requirements.txt）
sherpa-onnx>=1.10.30   # Apache-2.0 推理运行时
sounddevice            # MIT 录音
numpy                  # BSD-3
pynput                 # LGPL-3.0 全局热键
pypinyin               # MIT 拼音匹配
uiautomation           # Apache-2.0 焦点文本框读写
comtypes>=1.4.8        # MIT（uiautomation 依赖）
pystray                # LGPL-3.0 托盘图标
pillow                 # MIT-CMU 图标绘制（pystray 依赖）
# 可选：微软晓伊神经语音播报（edge-tts 为 GPL-3.0，不随本项目分发，想用自行安装；
# 未安装或断网时自动用系统 SAPI 语音）
# pip install edge-tts
